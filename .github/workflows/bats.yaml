on:
  push: {}
  pull_request: {}

name: "Test"
permissions: {}

jobs:
  sh-checker:
    runs-on: ubuntu-latest
    steps:
      - name: Setup BATS
        uses: mig4/setup-bats@v1
        with:
          bats-version: 1.8.2
      - uses: actions/checkout@v3
      - name: Test
        run:  bats test/*.bats --filter-tags short